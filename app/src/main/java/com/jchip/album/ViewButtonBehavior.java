package com.jchip.album;

import android.content.Context;
import android.util.AttributeSet;

import com.jchip.album.photo.behavior.FatButtonScrollHideBehavior;

public class ViewButtonBehavior extends FatButtonScrollHideBehavior {
    public ViewButtonBehavior(Context context, AttributeSet attributeSet) {
        super(context, attributeSet);
    }
}
