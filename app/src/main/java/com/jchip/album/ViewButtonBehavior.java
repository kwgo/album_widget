package com.jchip.album;

import android.content.Context;
import android.util.AttributeSet;

import com.jchip.album.photo.common.FatButtonBehavior;

public class ViewButtonBehavior extends FatButtonBehavior {
    public ViewButtonBehavior(Context context, AttributeSet attributeSet) {
        super(context, attributeSet);
    }
}
